name: Weekly Sync

on:
  schedule:
    - cron: '0 0 * * 0'
  workflow_dispatch:

jobs:
  weekly:
    uses: clawos-federation/clawos-actions/.github/workflows/weekly-sync.yml@main
    secrets: inherit
