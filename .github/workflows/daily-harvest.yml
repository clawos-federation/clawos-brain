name: Daily Harvest

on:
  schedule:
    - cron: '0 0 * * *'
  workflow_dispatch:

jobs:
  harvest:
    uses: clawos-federation/clawos-actions/.github/workflows/daily-harvest.yml@main
    secrets: inherit
