# 🏗️ ClawOS v1.0 完整架构设计文档

> **定位**: 基于 OpenClaw 的 AI 自治软件开发操作系统
> **核心理念**: 通用内核 + 垂直生态 + 持续进化
> **首个垂直领域**: Coding（软件开发）

---

## 一、项目总览

### 1.1 愿景

```
用户一句话 → 系统全自动开发 → 代码自动提交 GitHub → 持续迭代
```

### 1.2 核心原则

| 原则 | 说明 |
|------|------|
| **复用 OpenClaw** | 底层完全复用，不重复造轮子 |
| **通用内核** | 调度、通信、权限适用所有行业 |
| **垂直专精** | 每个领域独立生态，持续积累 |
| **Agent Native** | 系统为 Agent 设计，人只发意图 |
| **7×24 自治** | 长任务无人值守，主动汇报 |

---

## 二、分层架构：OpenClaw + ClawOS

```
┌─────────────────────────────────────────────────────────────┐
│                    ClawOS 上层（原创）                        │
│  ┌─────────────────────────────────────────────────────┐   │
│  │              通用内核 (Core)                          │   │
│  │  GM Agent + PM Agent + Assistant + Gateway API      │   │
│  │  Agent Registry + 调度引擎 + 权限系统                 │   │
│  └─────────────────────────────────────────────────────┘   │
│                           ↓                                 │
│  ┌─────────────────────────────────────────────────────┐   │
│  │            垂直生态层 (Ecosystems)                    │   │
│  │  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐  │   │
│  │  │ Coding Crew │  │ Legal Crew  │  │ Video Crew  │  │   │
│  │  │ (Phase 1)   │  │ (Phase 2)   │  │ (Phase 3)   │  │   │
│  │  └─────────────┘  └─────────────┘  └─────────────┘  │   │
│  └─────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
                           ↓ 调用
┌─────────────────────────────────────────────────────────────┐
│                 OpenClaw 底层（直接复用）                     │
│  Node 通信 + P2P/加密 + 沙箱 + 设备控制 + Skill 市场         │
│  sessions_spawn + 多模型路由 + 权限基础                      │
└─────────────────────────────────────────────────────────────┘
```

---

## 三、通用内核设计 (ClawOS Core)

### 3.1 角色体系

```
┌─────────────────────────────────────────────────────────────┐
│                     用户 (User)                              │
│                   只提需求、验收结果                          │
└─────────────────────────┬───────────────────────────────────┘
                          │
┌─────────────────────────▼───────────────────────────────────┐
│               Assistant Agent (助理)                         │
│  - 唯一与人交互入口                                          │
│  - 主动汇报进度、异常、完成                                   │
│  - 情绪友好、心理学优化                                       │
│  - 不执行技术任务                                            │
└─────────────────────────┬───────────────────────────────────┘
                          │
┌─────────────────────────▼───────────────────────────────────┐
│                  GM Agent (总经理)                           │
│  - 系统最高决策中枢                                          │
│  - 识别任务类型 → 路由到对应 PM                              │
│  - 任命 PM、授权资源                                         │
│  - 最终验收成果                                              │
│  - 唯一管理 Gateway 的角色                                   │
└─────┬──────────┬──────────┬──────────┬─────────────────────┘
      │          │          │          │
┌─────▼─────┐┌───▼────┐┌────▼────┐┌────▼────┐
│ Platform  ││ Dev PM ││ Legal PM││ Video PM│ ...
│    PM     ││(Phase1)││(Phase2) ││(Phase3) │
└─────┬─────┘└───┬────┘└────┬────┘└────┬────┘
      │          │          │          │
   造 Agent   组建开发    组建法律    组建视频
   维护生态    团队        团队        团队
```

### 3.2 PM 分为两类

| PM 类型 | 职责 | 生命周期 |
|---------|------|----------|
| **Platform PM** | 造 Agent、建 Skill、维护 Registry、接入 GitHub/OpenClaw | 永久存在 |
| **Project PM** | 接项目、组队、推进、质量、汇报 | 按项目创建/销毁 |

### 3.3 Gateway 内核化

```typescript
// Gateway 变为纯 API 服务，无界面
interface GatewayAPI {
  // Agent 管理
  createAgent(config: AgentConfig): AgentID;
  destroyAgent(id: AgentID): void;
  listAgents(): Agent[];
  
  // 任务调度
  submitTask(task: Task): TaskID;
  getTaskStatus(id: TaskID): TaskStatus;
  
  // 资源访问
  callLLM(model: string, prompt: string): Response;
  callSkill(skill: string, params: any): Result;
  callNode(node: string, command: string): Result;
  
  // 通信
  sendMessage(from: AgentID, to: AgentID, message: any): void;
  broadcast(topic: string, message: any): void;
  
  // 权限
  checkPermission(agent: AgentID, resource: string): boolean;
  grantPermission(agent: AgentID, resource: string): void;
}
```

### 3.4 Agent Registry（人才库）

```typescript
interface AgentRegistry {
  // Agent 模板库
  templates: {
    [name: string]: AgentTemplate;
  };
  
  // 活跃实例
  instances: {
    [id: string]: AgentInstance;
  };
  
  // 按领域分类
  categories: {
    coding: AgentTemplate[];
    legal: AgentTemplate[];
    video: AgentTemplate[];
    // ...
  };
}

interface AgentTemplate {
  id: string;
  name: string;
  category: string;
  
  // 八件套
  llm: string;           // 绑定的模型
  skills: string[];      // 技能列表
  tools: string[];       // 工具列表
  knowledge: string[];   // 知识库
  memory: MemoryConfig;  // 记忆配置
  hooks: Hook[];         // 触发器
  permissions: string[]; // 权限边界
  
  // 元数据
  version: string;
  author: string;
  rating: number;
  usageCount: number;
}
```

---

## 四、Coding 领域垂直生态 (Phase 1)

### 4.1 Dev PM 的团队

```
Dev PM (项目经理)
    │
    ├── Product Agent (产品)
    │   └── 需求梳理、用户故事、验收标准
    │
    ├── Architect Agent (架构)
    │   └── 技术方案、模块划分、接口设计
    │
    ├── Frontend Agent (前端)
    │   └── UI开发、组件、样式
    │
    ├── Backend Agent (后端)
    │   └── API、逻辑、数据库
    │
    ├── Test Agent (测试)
    │   └── 单元测试、集成测试、E2E
    │
    ├── Review Agent (代码审查)
    │   └── 代码质量、安全检查、最佳实践
    │
    ├── DevOps Agent (运维)
    │   └── CI/CD、部署、监控
    │
    └── GitHub Agent (协作)
        └── PR、Issue、版本发布
```

### 4.2 专用 Skills 库

```yaml
# Coding 领域专用技能
coding_skills:
  # 语言专精
  languages:
    - python-expert
    - javascript-expert
    - typescript-expert
    - go-expert
    - rust-expert
    
  # 框架专精
  frameworks:
    - react-dev
    - vue-dev
    - nextjs-dev
    - fastapi-dev
    - django-dev
    
  # 开发工具
  tools:
    - git-operations
    - docker-deploy
    - kubernetes-config
    - testing-frameworks
    - linting-formatting
    
  # 代码分析
  analysis:
    - code-review
    - security-scan
    - performance-profile
    - dependency-check
    
  # GitHub 集成
  github:
    - create-pr
    - review-pr
    - manage-issues
    - release-version
    - sync-fork
```

### 4.3 专用知识库

```yaml
# Coding 领域知识库
coding_knowledge:
  # 设计模式
  patterns:
    - gang-of-four-patterns
    - enterprise-patterns
    - reactive-patterns
    - microservices-patterns
    
  # 最佳实践
  practices:
    - clean-code
    - solid-principles
    - tdd-guide
    - ci-cd-best-practices
    
  # 安全规范
  security:
    - owasp-top-10
    - secure-coding-guide
    - api-security
    
  # 项目特定
  project_specific:
    - codebase-history
    - api-documentation
    - architecture-decisions
```

### 4.4 工作流程

```
用户 → Assistant: "帮我开发一个 URL 缩短服务"
        │
        ▼
Assistant → GM: 结构化需求
        │
        ▼
GM: 识别为开发任务 → 任命 Dev PM
        │
        ▼
Dev PM:
  1. 从 Registry 挑选团队
     - Product Agent
     - Architect Agent
     - Backend Agent
     - Frontend Agent
     - Test Agent
     - GitHub Agent
        │
        ▼
  2. 组织项目会议
     - Product: 需求文档
     - Architect: 技术方案
     - 拆分任务、分配工作
        │
        ▼
  3. 并行开发
     - Backend: API 开发
     - Frontend: UI 开发
     - Test: 测试编写
        │
        ▼
  4. 代码审查
     - Review Agent: 检查质量
     - Security Agent: 安全扫描
        │
        ▼
  5. 自动提交
     - GitHub Agent: 创建 PR
     - 自动合并、发布版本
        │
        ▼
Dev PM → GM: 成果汇报
        │
        ▼
GM → Assistant: 验收通过
        │
        ▼
Assistant → 用户: "项目完成！代码已提交到 GitHub..."
```

---

## 五、与 OpenClaw 的复用关系

### 5.1 直接复用的能力

| OpenClaw 模块 | 复用方式 | 说明 |
|--------------|---------|------|
| **Node 通信** | npm 依赖 | P2P、加密、隧道 |
| **设备控制** | 直接调用 | 远程执行 |
| **沙箱隔离** | 直接使用 | 安全执行环境 |
| **Skill 市场** | ClawHub | 社区技能直接用 |
| **多模型路由** | 直接使用 | GPT/Claude/Gemini |
| **sessions_spawn** | 扩展 | 作为 Agent 创建基础 |
| **权限基础** | 扩展 | 升级为 Agent 级 |

### 5.2 需要新增的模块

| 新模块 | 功能 | 优先级 |
|--------|------|--------|
| **Agent Registry** | 模板库 + 实例管理 | P0 |
| **GM Agent** | 决策中枢 | P0 |
| **Assistant Agent** | 人机交互 | P0 |
| **Platform PM** | 生态维护 | P0 |
| **Dev PM + 团队** | 开发能力 | P0 |
| **Gateway API** | 内核化 | P1 |
| **任务调度引擎** | 自动化 | P1 |
| **GitHub 深度集成** | 代码协作 | P1 |

### 5.3 更新策略

```yaml
# 自动跟随 OpenClaw 更新
openclaw_dependency:
  source: npm
  package: openclaw
  update_strategy: auto
  version: ">=2026.2.19"
  
# 测试策略
testing:
  on_update: run_integration_tests
  rollback_on_failure: true
```

---

## 六、GitHub 深度集成

### 6.1 双向同步

```
GitHub ───────────────────────────────────────► ClawOS
  │                                              │
  │ 导入:                                        │ 导出:
  │ - Agent 组件                                 │ - 代码
  │ - Skills 市场                                │ - 文档
  │ - Prompt 库                                  │ - 架构图
  │ - 开源项目                                   │ - 版本发布
  │                                              │
  ◄──────────────────────────────────────────────
```

### 6.2 自动化流程

```yaml
# GitHub 事件触发
triggers:
  on_issue:
    - auto_assign_to_pm
    - create_task
    - notify_assistant
    
  on_pr:
    - auto_review
    - run_tests
    - quality_check
    
  on_star:
    - thank_user  # 通过 Assistant
    
  on_release:
    - update_docs
    - notify_users
```

### 6.3 GitHub 作为外部资源

```typescript
interface GitHubIntegration {
  // 搜索复用
  searchAgents(query: string): AgentTemplate[];
  searchSkills(query: string): Skill[];
  searchPrompts(query: string): Prompt[];
  
  // 代码协作
  createRepo(name: string): Repo;
  createPR(repo: string, changes: Changes): PR;
  mergePR(pr: PR): void;
  
  // 项目管理
  createIssue(repo: string, issue: Issue): void;
  createProject(name: string): Project;
}
```

---

## 七、质量保障机制

### 7.1 多层审查

```
执行 Agent 完成
    │
    ▼
自检 (Self-Check)
    │
    ▼
交叉审查 (Peer Review)
    │
    ▼
PM 审核 (PM Review)
    │
    ▼
GM 验收 (GM Acceptance)
    │
    ▼
通过 → 交付
```

### 7.2 长任务管理

```typescript
interface LongTask {
  id: string;
  type: 'book' | 'project' | 'migration';
  
  // 里程碑
  milestones: {
    name: string;
    status: 'pending' | 'running' | 'done' | 'failed';
    deadline: Date;
  }[];
  
  // 检查点
  checkpoints: {
    time: Date;
    progress: number;
    artifacts: string[];
  }[];
  
  // 汇报策略
  reportStrategy: {
    onMilestone: boolean;     // 里程碑完成时汇报
    onBlocker: boolean;       // 阻塞时汇报
    periodicHours: number;    // 定期汇报间隔
    onCompletion: boolean;    // 完成时汇报
  };
}
```

---

## 八、开发路线图

### Phase 1: 通用内核 + Coding MVP (4 周)

| 周 | 内容 | 产出 |
|----|------|------|
| W1 | Agent Registry + GM Agent | 基础调度能力 |
| W2 | Assistant Agent + 通信层 | 人机交互入口 |
| W3 | Dev PM + 核心 Agents | 开发团队能力 |
| W4 | GitHub 集成 + 测试 | 端到端流程 |

### Phase 2: Coding 生态完善 (4 周)

| 周 | 内容 | 产出 |
|----|------|------|
| W5 | 更多语言/框架 Skills | 覆盖主流技术栈 |
| W6 | 知识库建设 | 设计模式、最佳实践 |
| W7 | 复杂项目能力 | 多模块、微服务 |
| W8 | 质量保障强化 | 多层审查、长任务 |

### Phase 3: 垂直领域扩展

| 阶段 | 领域 | 内容 |
|------|------|------|
| Phase 3 | 法律 | Legal PM + 合同审查 + 法规检索 |
| Phase 4 | 视频 | Video PM + 剪辑 + 特效 |
| Phase 5 | 金融 | Finance PM + 分析 + 报告 |
| ... | ... | 按需扩展 |

---

## 九、技术选型

### 9.1 模型分配

| 角色 | 推荐模型 | 原因 |
|------|---------|------|
| GM | claude-opus-4-6-thinking | 最强推理 |
| PM | claude-sonnet-4-5 | 平衡能力 |
| Architect | claude-sonnet-4-5 | 架构设计 |
| Dev Agent | gpt-5.3-codex | 代码专精 |
| Test Agent | gemini-3-flash | 快速测试 |
| Assistant | glm-5 | 经济高效 |

### 9.2 数据存储

| 类型 | 存储 | 说明 |
|------|------|------|
| Agent Registry | PostgreSQL | 结构化查询 |
| 知识库 | Vector DB | 语义检索 |
| 任务日志 | ClickHouse | 时序分析 |
| 代码产物 | Git/GitHub | 版本控制 |

---

## 十、最终架构图

```
                    ┌──────────────────┐
                    │      用户         │
                    └────────┬─────────┘
                             │ 自然语言
                    ┌────────▼─────────┐
                    │  Assistant Agent │
                    │  (主动汇报/友好)  │
                    └────────┬─────────┘
                             │
┌────────────────────────────▼────────────────────────────┐
│                    GM Agent (CEO)                        │
│          识别任务 → 任命 PM → 授权 → 验收               │
└─────┬──────────┬──────────┬──────────┬─────────────────┘
      │          │          │          │
┌─────▼─────┐┌───▼────┐┌────▼────┐┌────▼────┐
│ Platform  ││ Dev PM ││  ...    ││  ...    │
│    PM     ││ (P1)   ││ (P2+)   ││ (P3+)   │
└─────┬─────┘└───┬────┘└─────────┘└─────────┘
      │          │
   造 Agent   ┌──▼──────────────────────────┐
   维护生态   │     Coding Crew (Phase 1)    │
             │  ┌────────┐  ┌────────┐      │
             │  │Product │  │Architect│     │
             │  └────────┘  └────────┘      │
             │  ┌────────┐  ┌────────┐      │
             │  │Frontend│  │Backend │      │
             │  └────────┘  └────────┘      │
             │  ┌────────┐  ┌────────┐      │
             │  │  Test  │  │ GitHub │      │
             │  └────────┘  └────────┘      │
             └──┬───────────────────────────┘
                │
┌───────────────▼───────────────────────────────────────────┐
│                    Gateway (内核)                          │
│  Agent Registry │ 调度引擎 │ 通信 │ 权限 │ 知识库        │
└───────────────┬───────────────────────────────────────────┘
                │
┌───────────────▼───────────────────────────────────────────┐
│                  OpenClaw (底层复用)                       │
│  Node │ P2P/加密 │ 沙箱 │ 设备控制 │ Skills │ 多模型      │
└───────────────────────────────────────────────────────────┘
                │
┌───────────────▼───────────────────────────────────────────┐
│                    外部生态                                │
│  GitHub (代码) │ ClawHub (Skills) │ 知识库 │ API         │
└───────────────────────────────────────────────────────────┘
```

---

## 十一、下一步行动

### 立即可做

1. **创建项目目录**
```bash
mkdir -p clawos/{core,ecosystems/coding,docs}
```

2. **定义 Agent Registry Schema**
3. **实现 GM Agent 原型**
4. **实现 Assistant Agent 原型**
5. **实现 Dev PM + 核心团队**

### 验证目标

**第一个可跑版本**: 用户说一句话 → 系统自动写出一个 URL 缩短服务 → 代码提交 GitHub

---

*文档版本: v1.0*
*创建时间: 2026-02-23*
*状态: 待讨论确认*
