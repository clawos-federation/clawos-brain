# AGENTS.md - ClawOS 完整架构手册

> **版本**: 2026.3.5 (v5) | **更新**: 2026-02-27
> **核心范式**: 固定角色模板 + 动态实例化 | 记忆绑定角色非实例
> **v5 新特性**: 身份系统 + A2A 协议 + 进化调度 + 风险控制
---

## 一句话定义

**ClawOS 是建立在 OpenClaw 之上的分布式 AI 文明：每个节点是一个专业器官，GitHub 是共同的 DNA，记忆系统是神经网络，进化引擎让整体持续成长——人只需要掌舵方向。**

---

## 系统全景图

```
┌─────────────────────────────────────────────────────────────────┐
│                        四条入口链路                              │
├─────────────────────────────────────────────────────────────────┤
│ 链路A: Terminal L0 → openclaw agent → GM → PM → Workers         │
│ 链路B: WhatsApp/Telegram → assistant → gm → 结果                │
│ 链路C: OpenClaw Cron/Webhook 自动触发                           │
│ 链路D: Alpha 量化 → Blackboard → L0 决策                        │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                   Command Layer (3)                              │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐                      │
│  │assistant │  │    gm    │  │validator │                      │
│  │ (入口)   │→ │ (决策)   │← │ (验证)   │                      │
│  │  glm-5   │  │ opus-4-6 │  │ opus-4-6 │                      │
│  └──────────┘  └──────────┘  └──────────┘                      │
└─────────────────────────────────────────────────────────────────┘
                              │ GM 只调用 PM 层
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                     PM Layer (4)                                 │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐        │
│  │platform  │  │  coding  │  │ writing  │  │ research │        │
│  │   pm     │  │    pm    │  │    pm    │  │    pm    │        │
│  │  codex   │  │  codex   │  │  codex   │  │  codex   │        │
│  └──────────┘  └──────────┘  └──────────┘  └──────────┘        │
└─────────────────────────────────────────────────────────────────┘
                              │ PM 调度 Workers
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                     Workers (12)                                │
│  ┌────────────┐  ┌────────────┐  ┌────────────┐  ┌────────────┐│
│  │   开发组    │  │   写作组    │  │   系统组    │  │   服务组    ││
│  │ (codex)    │  │  (glm-5)   │  │ (codex)    │  │  (glm-5)   ││
│  ├────────────┤  ├────────────┤  ├────────────┤  ├────────────┤│
│  │frontend    │  │writer      │  │sreagent    │  │browser     ││
│  │backend     │  │reviewer    │  │security    │  │ -worker    ││
│  │tester      │  │            │  │github-ops  │  │ (共享)     ││
│  │            │  │            │  │alpha-bridge│  │            ││
│  └────────────┘  └────────────┘  └────────────┘  └────────────┘│
│  ┌────────────┐                                                 │
│  │   调研组    │  ← 新增 (research-pm 专用)                      │
│  │  (glm-5)   │                                                 │
│  ├────────────┤                                                 │
│  │researcher  │                                                 │
│  │  -web      │                                                 │
│  │researcher  │  ← 新增                                         │
│  │  -file     │                                                 │
│  └────────────┘                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🆕 v5 新特性 (2026-02-27)

### 身份系统 (Identity System)
- **DID 文档**: 每个节点都有 W3C DID (`did:web:clawos.local:*`)
- **Ed25519 密钥对**: 用于签名验证
- **Profile**: 节点专长、能力、预算配置
- **Wallet**: 本地类型，预算限制

```
clawos/identity/
├── nodes/{server,coding,writing,quant}/
│   ├── did.json         # W3C DID 文档
│   ├── profile.json     # 节点配置
│   └── wallet.json      # 预算和权限
└── federation/
    ├── master-identity.json
    └── trust-registry.json
```

### A2A 协议 (Agent-to-Agent)
- **Agent Cards**: JSON Schema Draft 2020-12 标准卡片
- **14 张卡片**: Command(4) + PM(3) + Workers(7)
- **ID 模式**: `openclaw/{tier}/{name}` (3段式)
- **验证脚本**: `a2a/schema/card-validation-script.py`

### 进化调度器 (Evolution Scheduler)
- **P1 知识更新**: 15分钟空闲
- **P2 能力训练**: 1小时空闲
- **P3 领域探索**: 2小时空闲
- **P4 SOUL草案**: 4小时空闲
- **真实任务优先**: 立即中断进化任务

### 效用评分 (Utility Scoring)
- **19 个 Agent**: 每个都有 0.0-1.0 评分
- **提名阈值**: ≥0.85 可提名进入联邦记忆
- **警告阈值**: <0.50 需要审查
- **反馈收集**: 每次任务完成后收集验证反馈

### 风险控制器 (Risk Controller)
- **5 条规则**: 包含 3 条不可变规则
- **Alpha 隔离**: alpha-executor 只能在 quant 节点运行
- **SDK 级强制**: 在 GM 调度前强制检查
- **不可变保护**: 交易限制、断连保护不可修改

### 四层记忆系统 (L1-L4)
| 层级 | 类型 | 容量 | 用途 |
|------|------|------|------|
| L1 | Session | 100MB | RAM 级会话记忆 |
| L2 | History | ~1GB | SQLite 任务历史 |
| L3 | Vector | 无限 | 经验向量存储 |
| L4 | GitHub | 无限 | 跨机器同步 |

---

## 19 Agents 完整配置

### Command Layer (3)

| Agent | 职责 | 模型 | 成本 |
|-------|------|------|------|
| **assistant** | 唯一人机交互入口、接收意图、主动汇报 | glm-5 | 免费 |
| **gm** | 总决策中枢、任务评估、PM 任命、最终验收 | opus-4-6 | 付费 |
| **validator** | 质量验证、独立审计 | opus-4-6 | 付费 |

### PM Layer (4)

| Agent | 职责 | 管理的 Workers | 模型 |
|-------|------|----------------|------|
| **platform-pm** | 系统维护、生态管理 | github-ops, sreagent, securityagent, browser-worker | gpt-5.3-codex |
| **coding-pm** | 开发管理 | coder-frontend, coder-backend, tester-auto, browser-worker | gpt-5.3-codex |
| **writing-pm** | 写作管理 | writer-general, reviewer-content, browser-worker | gpt-5.3-codex |
| **research-pm** | 调研管理 | researcher-web, researcher-file, browser-worker | gpt-5.3-codex |

### Workers (12)

**开发组 (codex-oauth):**

| Agent | 职责 | 模型 |
|-------|------|------|
| coder-frontend | 前端开发 | codex-oauth |
| coder-backend | 后端开发 | codex-oauth |
| tester-auto | 测试编写 | codex-oauth |

**写作组 (glm-5):**

| Agent | 职责 | 模型 |
|-------|------|------|
| writer-general | 内容写作 | glm-5 |
| reviewer-content | 内容审核 | glm-5 |

**系统组 (codex-oauth):**

| Agent | 职责 | 模型 |
|-------|------|------|
| sreagent | 运维专家 | codex-oauth |
| securityagent | 安全专家 | codex-oauth |
| github-ops | Git 操作 | codex-oauth |
| alpha-bridge | Alpha 系统通信桥梁 | glm-5 |

**调研组 (glm-5):**

| Agent | 职责 | 模型 |
|-------|------|------|
| researcher-web | 网络调研分析 | glm-5 |
| researcher-file | 文件读取与摘要 | glm-5 |

**服务组 (glm-5) - 多 PM 共享:**

| Agent | 职责 | 模型 | 调用方 |
|-------|------|------|--------|
| **browser-worker** | 浏览器自动化、数据抓取、截图 | glm-5 | platform-pm, coding-pm, writing-pm, research-pm |

---

## Worker 边界定义

### 开发组 - 做的事
- ✅ 编写前端/后端代码
- ✅ 编写测试用例
- ✅ 代码重构和优化
- ✅ Bug 修复
- ✅ 技术文档编写

### 开发组 - 不做的事
- ❌ 直接与用户交互
- ❌ 跨组任务编排
- ❌ 生产环境部署（归 sreagent）
- ❌ 安全审计（归 securityagent）

### 写作组 - 做的事
- ✅ 内容创作
- ✅ 内容审核
- ✅ 文档撰写

### 写作组 - 不做的事
- ❌ 代码生成
- ❌ 系统配置
- ❌ 技术实现决策

### 系统组 - 做的事
- ✅ Git 操作
- ✅ 部署和运维
- ✅ 安全审计
- ✅ 系统监控

### 系统组 - 不做的事
- ❌ 业务代码开发
- ❌ 内容创作
- ❌ 产品决策

### 服务组 (browser-worker) - 做的事
- ✅ 网页自动化（导航、点击、填写）
- ✅ 数据抓取和提取
- ✅ 截图和页面分析
- ✅ 多标签页管理
- ✅ 会话状态保持

### 服务组 (browser-worker) - 不做的事
- ❌ 直接与用户交互
- ❌ 业务代码开发
- ❌ 安全审计（归 securityagent）
- ❌ 生产部署（归 sreagent）

### 调研组 - 做的事
- ✅ 信息收集和分析
- ✅ 网络搜索和调研
- ✅ 文件读取与摘要生成
- ✅ 数据整理和报告

### 调研组 - 不做的事
- ❌ 代码开发
- ❌ 内容创作（归写作组）
- ❌ 系统配置

---

## 范式B：固定角色模板 + 动态实例化

### 核心原则

| 原则 | 说明 |
|------|------|
| **角色是永久的** | SOUL.md 模板存 GitHub，永不销毁 |
| **实例是临时的** | 任务来了实例化，完成后销毁 |
| **记忆是桥梁** | 经验通过记忆系统传承给下一个实例 |

### 实例化流程

```bash
# PM 召唤 Worker
openclaw spawn \
  --role researcher-file \
  --memory-load role:researcher-file \
  --context task:{taskId} \
  --session pipeline:{taskId}:researcher-file
```

### 记忆归还流程

```
Worker 完成任务
    ↓
EverMemOS 提炼情节记忆
    ↓
MemOS 合并入角色记忆
    ↓
写回 GitHub memory/
    ↓
实例销毁
    ↓
下一个同类实例自动继承记忆
```

---

## 记忆系统架构 (四层)

### 核心约束

| 规则 | 说明 |
|------|------|
| 禁止直接读文件 | GM 不直接读取文件内容 |
| 只调度 PM 层 | GM 只指挥 PM，不直接指挥 Workers |
| 最多 5k tokens 上下文 | 要求 PM 返回精简摘要 |

### 信息获取流程

```
GM 需要信息
    ↓
指派 research-pm
    ↓
research-pm 先读 summary.md
    ↓
不够？派 researcher-file 读原文件
    ↓
返回摘要给 GM（<5k tokens）
```

### GM 可调用的 PM

| PM | 职责 |
|----|------|
| `research-pm` | 信息收集协调 |
| `coding-pm` | 开发任务协调 |
| `writing-pm` | 写作任务协调 |
| `platform-pm` | 系统运维协调 |
| `validator` | 独立质量验证 |

---

## 双通路人机交互 (v2)

| 通路 | 入口 | 适合场景 |
|------|------|---------|
| **通路A：移动端** | Telegram / WhatsApp | 碎片需求，进度通知 |
| **通路B：终端** | Claude Code (L0) | 深度工作，复杂任务 |

### Claude Code 特殊能力 (相比 assistant)
- 直接读写 blackboard，不经过 assistant 转发
- 可以绕过 assistant 直接召唤 gm 甚至特定 PM
- 可以审查任务执行细节，直接干预流程

---

### 链路A: Terminal L0 (开发主导)
```
我(L0) → openclaw agent --agent gm → PM → Workers
```
- **场景**: 在终端直接指挥
- **L0 角色**: 主导者
- **不做**: 不直接执行物理操作

### 链路B: 消息 App (用户远程)
```
用户 → WhatsApp/Telegram → assistant → gm → 结果
```
- **场景**: 用户通过消息 App 交互
- **L0 角色**: 观察者
- **不做**: 不插手 assistant 的工作

### 链路C: 全自动 (Cron/Webhook)
```
OpenClaw Cron/Webhook 自动触发
```
- **场景**: 定时任务或事件触发
- **L0 角色**: 接收者
- **不做**: 不干预自动流程

### 链路D: Alpha 上报 (量化系统)
```
Alpha 量化完成 → 写 Blackboard → L0 看报告决策
```
- **场景**: Alpha 系统完成量化任务
- **L0 角色**: 决策者
- **不做**: 不绕过 risk-controller

---

## GitHub DNA 体系

### 仓库结构

```
clawos-templates (jajabong/)
├── souls/                    # 角色模板 (DNA)
│   ├── command/             # Command 层
│   ├── pm/                 # PM 层
│   └── workers/            # Worker 矩阵
│       ├── analyst/
│       ├── creator/
│       ├── critic/
│       ├── executor/
│       └── connector/
├── memory/                  # 角色记忆 (层4)
├── skills/                  # 通用技能
├── workflows/              # Lobster 模板
├── knowledge/              # 核心知识
└── registry/
    └── capabilities.json   # 角色注册表 (PM 选角用)
```

### capabilities.json 作用

PM 通过 `capabilities.json` 做语义匹配选角色：
```json
{
  "id": "researcher-file",
  "capabilities": ["文件读取", "代码分析", "摘要生成"],
  "performance": {"avg_score": 8.5, "tasks_completed": 12}
}
```

---

## 进化引擎

### 三层节奏

| 节奏 | 执行者 | 内容 |
|------|--------|------|
| 每天 03:00 | 自动 | 知识收割，更新 skills/ |
| 每周 | platform-pm | 角色记忆提炼，性能报告 |
| 每月 | Boss | 角色淘汰/引入决策 |

### 角色进化路径

```
新角色 (无记忆)
    ↓ 完成任务
成熟角色 (有记忆，性能稳定)
    ↓ 发现新模式
专家角色 (深度记忆，跨域影响)
    ↓ 经验提炼
传承 (角色记忆沉淀为 DNA)
```

---

## Blackboard 目录结构 (v5)

```
~/clawos/blackboard/
├── tasks/{taskId}/
│   ├── task.json              # 任务定义
│   ├── team-composition.json  # PM 选角决策
│   ├── status.md              # 实时进度
│   ├── result.json            # 最终产出
│   ├── validation.json        # Validator 评分
│   └── memory-harvest.json    # 记忆提炼摘要
├── gm/
│   ├── decisions.md           # GM 决策日志
│   └── pm-performance.json    # PM 历史表现
├── roles/                     # 角色实例状态
│   ├── active.json           # 当前活跃实例
│   └── memory-queue.json      # 待归还记忆队列
├── federation/                # 🆕 v5 联邦系统
│   └── evolution-queue/       # 进化调度队列
│       ├── schema.json        # 队列 schema
│       ├── p1-knowledge.json  # P1 知识更新
│       ├── p2-training.json   # P2 能力训练
│       ├── p3-exploration.json# P3 领域探索
│       ├── p4-soul-drafts.json# P4 SOUL 草案
│       └── scheduler-state.json# 调度状态
├── utility-scores/            # 🆕 v5 效用评分
│   └── {agent}.json           # 19 个 Agent 评分
├── escalations/               # 紧急上报
├── shared/
│   ├── node-status.json      # 节点状态
│   └── risk-limits.json      # 风控规则
├── alpha/                     # Alpha 量化数据
├── metrics/                   # 🆕 v5 系统指标
└── persistence/               # 持久化存储
```

---

## 每次会话清单

1. **读取 SOUL.md** — 这是你的核心特质
2. **读取 Blackboard** — 检查 `~/clawos/blackboard/` 的相关状态
3. **检查任务** — 读取分配给你的任务目录

---

## 文件引用

| 需求 | 文件 |
|------|------|
| 核心特质 | `@workspace/SOUL.md` |
| L0 协议 | `@CLAUDE.md` |
| Agent 配置 | `~/.openclaw/openclaw.json` |
| SOUL 文件 | `clawos/souls/*/SOUL.md` |
| 角色注册表 | `clawos-templates/registry/capabilities.json` |
| 记忆系统 | `clawos/services/EverMemOS/` |
| 🆕 身份系统 | `clawos/identity/nodes/*/did.json` |
| 🆕 A2A 卡片 | `clawos/a2a-cards/**/*.json` |
| 🆕 功能开关 | `clawos/config/features.json` |
| 🆕 效用评分 | `clawos/blackboard/utility-scores/` |
| 🆕 进化队列 | `clawos/blackboard/federation/evolution-queue/` |
| 🆕 风控规则 | `clawos/blackboard/shared/risk-limits.json` |
---

## 实施计划

| Phase | 目标 | 状态 |
|-------|------|------|
| Phase 0.5 | 基础稳定化 - Feature flags, 回滚文档 | ✅ 完成 |
| Phase 1.1 | DID 身份系统 - 4 节点加密身份 | ✅ 完成 |
| Phase 1.2-1.3 | 节点 Profile + Wallet Stub | ✅ 完成 |
| Phase 2.1 | A2A Schema - JSON Schema for Agent Cards | ✅ 完成 |
| Phase 2.2 | Agent Cards - 14 张卡片 | ✅ 完成 |
| Phase 3 | Evolution Queue - P1-P4 优先级调度 | ✅ 完成 |
| Phase 4 | Utility Score - 19 个 Agent 评分 | ✅ 完成 |
| Phase 5 | Memory L1-L4 - 四层记忆系统 | ✅ 完成 |
| Phase 6 | Risk Controller - 5 条规则 (3 不可变) | ✅ 完成 |
| Phase 7 | Enhanced GM - 联邦路由器 | ✅ 完成 |
| **v5 发布** | **全部功能启用** | ✅ **2026-02-27** |

---

## Server/Alpha 节点边界 (v6)

### 设计模型：教练-球员 (Coach-Player)

Server 是教练，Alpha 是球员：
- **教练** 设计训练计划、观看比赛录像、给出技术指导
- **球员** 自己打比赛、自主决定战术、汇报成绩

### Server 节点职责 (Coordinator)

| 能力 | 说明 |
|------|------|
| `can_read_alpha_reports` | 读取 Alpha 日报、回测结果、信号预测 |
| `can_set_evolution_goals` | 设置 Alpha 进化方向 |
| `can_request_live_trading` | 请求开启实盘（需人工审批） |
| `can_emergency_halt` | 紧急停止 Alpha |
| `can_execute_trades` | ❌ 禁止 |
| `can_override_risk` | ❌ 禁止 |

### Alpha 节点职责 (Worker)

| 属性 | 值 |
|------|-----|
| `oversight_node` | server |
| `evolution_goals_source` | blackboard/evolution-queue/alpha-goals.json |
| `report_to` | blackboard/alpha/reports/ |
| `security.isolated` | true |
| `risk_controller_immutable` | true |

### 人工审批门控 (Human Approval Gate)

| 操作 | 需要审批 | 审批人 |
|------|----------|--------|
| 开启实盘交易 | ✅ 是 | human-operator |
| 重大策略变更 | ✅ 是 | human-operator |
| 紧急停止 | ❌ 否 | server 可直接执行 |
| 设置进化目标 | ❌ 否 | server 可直接执行 |

### 通信协议

```
Server → Alpha (目标设置):
  blackboard/evolution-queue/alpha-goals.json

Alpha → Server (结果汇报):
  blackboard/alpha/reports/
  blackboard/alpha/signals/

紧急控制 (secure channel):
  localhost:18790/control (mutual TLS)
```

### 参考文件

| 文件 | 说明 |
|------|------|
| `blackboard/shared/alpha-oversight.json` | 完整 oversight 策略 |
| `blackboard/shared/risk-limits.json` | 风控规则（含 live-trading-approval） |
| `identity/nodes/server/profile.json` | Server 节点配置 |
| `identity/nodes/alpha/profile.json` | Alpha 节点配置 |

---

## 功能开关 (Feature Flags)

| 功能 | 默认 | 描述 |
|------|------|------|
| identity-system | ✅ ON | 统一身份与加密签名 |
| a2a-registry | ✅ ON | Agent-to-Agent 通信注册 |
| evolution-scheduler | ✅ ON | 自动进化调度 |
| utility-scoring | ✅ ON | 贡献价值追踪 |
| memory-l3 | ✅ ON | 长期向量记忆 |
| risk-controller | ✅ ON | 风险评估与缓解 |

---

**ClawOS 2026.3.5 - 分布式自进化群体智能系统 v5**
