# ClawOS × OpenClaw 完整系统规划

> 基于 OpenClaw 真实架构的 Agent 自治操作系统
> 版本: v2.0 (修正版)

---

## 一、OpenClaw 真实架构（必须理解）

### OpenClaw 是什么

| 是 ✅ | 不是 ❌ |
|------|--------|
| 本地优先 AI 助理平台 | 分布式节点网络 |
| 单进程 Gateway（控制平面）| 微服务调度框架 |
| Session 管理 + Tool 调度 | P2P 加密隧道 |
| 多渠道消息路由 | 分布式任务队列 |

### 核心组件

| 组件 | 作用 |
|------|------|
| **Gateway** | 单一控制平面进程（18789端口）|
| **Agent** | 有独立 Workspace + Session + 工具权限的 AI 实例 |
| **Session** | 对话历史，JSONL 持久化 |
| **Skills** | SKILL.md + 脚本，教 Agent 使用工具 |
| **ClawHub** | Skill 注册中心（5700+ Skills）|
| **Lobster** | 原生工作流 Shell |
| **Canvas** | Agent 驱动可视化（18793端口）|
| **SOUL.md** | Agent 人格定义文件 |

### 关键约束

1. **Session 串行处理**：防止工具冲突
2. **Sub-agent 默认禁止**：`sessions_spawn` 需显式开启
3. **Skill 安全风险**：ClawHub 已发现 341+ 恶意 Skill
4. **单用户设计**：多租户需 Workspace 隔离
5. **Gateway 是单进程**：不是你自己写的调度器

---

## 二、ClawOS → OpenClaw 概念映射

| ClawOS 概念 | OpenClaw 实现 |
|-------------|--------------|
| Agent 实例 | `agents.list[]` 条目 + 独立 workspace |
| Agent 通信 | Webhook / 共享 Channel / 共享文件 |
| Agent 权限 | `permissions.allowedCommands` |
| Agent 人格 | `SOUL.md` + `AGENTS.md` |
| Agent 技能 | `skills/` 下的 SKILL.md |
| Agent 记忆 | Session JSONL + Memory 插件 |
| 任务流水线 | Lobster 工作流 |
| 定时任务 | Cron + Webhooks |

---

## 三、架构图（修正版）

```
┌─────────────────────────────────────────────────────────┐
│                    人（Boss）                            │
│         WhatsApp / Telegram / Discord / iMessage        │
└──────────────────────────┬──────────────────────────────┘
                           │ 消息输入
┌──────────────────────────▼──────────────────────────────┐
│              OpenClaw Gateway (Port 18789)               │
│   Channel 适配 → Binding 路由 → Session → Tool 调度     │
│          Cron / Webhooks / Events / Canvas              │
└──┬────────────┬────────────┬────────────┬───────────────┘
   │ Binding    │ Binding    │ Binding    │ Binding
   ▼            ▼            ▼            ▼
┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐
│Assistant │ │    GM    │ │Platform  │ │ Project  │
│  Agent   │ │  Agent   │ │   PM     │ │    PM    │
├──────────┤ ├──────────┤ ├──────────┤ ├──────────┤
│SOUL:友好 │ │SOUL:严谨 │ │SOUL:工程 │ │SOUL:管理 │
│Skills:   │ │Skills:   │ │Skills:   │ │Skills:   │
│-summarize│ │-evaluate │ │-clawhub  │ │-coord    │
│-notify   │ │-assign   │ │-build    │ │-qa       │
└──────────┘ └────┬─────┘ └──────────┘ └────┬─────┘
                  │ sessions_spawn            │
                  ▼                           ▼
         ┌────────────────────────────────────────┐
         │         Worker Agent 池                │
         │   writer / coder / researcher / ...    │
         │         (Pi Runner 执行)               │
         └────────────────────────────────────────┘
```

---

## 四、Agent 间通信方案

OpenClaw **原生不支持** Agent 直接互发消息，ClawOS 使用：

### 方案 A：Webhook 内部路由（推荐）

```
Worker 完成 → 调用 PM Webhook → PM 调用 GM Webhook → GM 通知 Assistant
```

### 方案 B：共享 Channel 消息总线

```
创建专用 Discord 频道 → 所有 Agent 监听/发送 → Binding 路由
```

### 方案 C：共享文件系统

```
Worker 写结果文件 → PM 轮询/监听 → 处理 → 写下一步文件
```

---

## 五、开发路线图

### Phase 1（2-3周）：最小闭环

**目标**：消息 → 助理 → GM → Worker → 助理通知

**任务**：
1. 安装配置 OpenClaw
2. 创建 Assistant Agent（SOUL.md + Binding）
3. 创建 GM Agent（SOUL.md + Webhook）
4. 编写 `gm-task-evaluate` Skill
5. 创建 Writer Worker
6. 编写最简 Lobster 工作流
7. 端到端测试

**不做**：多 Worker、ClawHub 同步、动态 PM

### Phase 2（4-6周）：组织能力

**目标**：GM 动态任命 PM，PM 组队，质检闭环

**任务**：
1. `pm-team-assemble` Skill
2. 开启 PM `sessions_spawn` 权限
3. 完整 Lobster 写书工作流
4. `progress-report` Skill
5. `quality-check` Skill
6. Platform PM ClawHub 同步
7. Canvas 进度看板

### Phase 3（持续）：生态与自治

**目标**：7×24 自治，自我扩展

**任务**：
1. 自动 ClawHub 同步
2. Agent 成功率统计
3. GitHub 双向集成
4. 多渠道支持
5. 语音交互
6. Nix 声明式配置

---

## 六、关键结论

**ClawOS 是建立在 OpenClaw Gateway 之上的 Agent 组织系统。**

- Gateway = 神经系统
- Lobster = 骨骼
- Skill = 肌肉
- SOUL.md = 灵魂
- ClawHub = 进化来源

**人只管提需求，其他的，让龙虾来。** 🦞

---

*版本: v2.0*
*创建时间: 2026-02-23*
